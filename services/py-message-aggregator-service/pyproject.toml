[project]
name = "py-message-aggregator-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.2",
    "infra",
    "config",
]

[tool.uv.sources]
infra = { path = "../../packages/infra" }
config = { path = "../../packages/config" }

